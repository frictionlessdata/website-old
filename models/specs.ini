[model]
name = Specs
label = Specs
hidden = yes
protected = yes

[children]
model = spec
order_by = -created, title

[fields.title]
label = Title
type = string
size = large

[fields.body]
label = Body
type = markdown
